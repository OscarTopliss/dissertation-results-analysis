This repository contains the raw dataset generated as part of my dissertation project, and 
the jupyter notebook I used to analyse its results.

To analyse the data yourself, set up a virtual environment with `python3 -m venv .venv; source .venv/bin/activate, pip install -r requirements.txt` and open this directory in a jupyter-enabled IDE like VSCode.
